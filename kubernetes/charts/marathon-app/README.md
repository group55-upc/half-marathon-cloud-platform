# MARATHON-APP

Helm Chart del backend de marathon app

El fitxer values.yaml conté els valors per defecte del chart

Al directory templates podem veure els .yaml que corresponen al deployment i al service de l'aplicació